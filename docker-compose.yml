version: '3.8'

services:
  poker-simulator:
    build: .
    container_name: poker-simulator
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - HOST=0.0.0.0
    restart: unless-stopped
    volumes:
      - ./data:/app/data
